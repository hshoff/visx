import type { BenchmarkCase } from './types';

/**
 * Base UI Charts is not published yet (`mui/base-ui-charts` is private).
 *
 * When `@base-ui/charts` becomes available, implement a LineChart adapter here and
 * register it as the first benchmark case to match Colm Tuite's original ordering.
 */
export const baseUiChartsCase: BenchmarkCase | null = null;
