# Changelog

## 1.0.0-alpha.0

- Initial alpha of `@visx/kernel`: structural memoization, accessors, domain helpers, path formatting, `toPath2D` with LRU cache and SSR shim, and development warnings behind `NODE_ENV`.
